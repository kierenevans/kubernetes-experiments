apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    ingress.kubernetes.io/secure-backends: "true"
  name: web
spec:
  rules:
  - host: demo-app-ssl-w.foo.continuouspipe.net
    http:
      paths:
      - backend:
          serviceName: web
          servicePort: 443
status:
  loadBalancer: {}
